/**
 *
 * @type {{show_drop_downList: boolean, options: *[], selected_option: {id: number, label: string}}}
 */
const select = {
  selected_option: { id: 2, label: 'Удаленности' },

  options: [
    { id: 0, label: 'Рейтингу' },
    { id: 1, label: 'Цене' },
    { id: 2, label: 'Удаленности' }
  ],

  show_drop_downList: false
}

export default select
